---
# Placeholder — replace encryptedData.admin-password with a real sealed value after bootstrap.
# Generate with:
#   kubectl create secret generic grafana-secret \
#     --from-literal=admin-password="$(openssl rand -base64 24)" \
#     --dry-run=client -o yaml -n ecoplan \
#     | kubeseal --format yaml > deploy/manifests/grafana/sealed-secret.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: grafana-secret
  namespace: ecoplan
spec:
  encryptedData:
    admin-password: REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      name: grafana-secret
      namespace: ecoplan
