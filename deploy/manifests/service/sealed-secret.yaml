---
# Placeholder — replace encryptedData values with real sealed secrets after bootstrap.
# Generate with:
#   kubectl create secret generic ecoscan-secret \
#     --from-literal=anthropic-api-key="sk-ant-..." \
#     --from-literal=ecoscan-api-key="..." \
#     --dry-run=client -o yaml -n ecoplan \
#     | kubeseal --format yaml > deploy/manifests/service/sealed-secret.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ecoscan-secret
  namespace: ecoplan
spec:
  encryptedData:
    anthropic-api-key: REPLACE_WITH_SEALED_VALUE
    ecoscan-api-key: REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      name: ecoscan-secret
      namespace: ecoplan
