---
# Placeholder — replace encryptedData.token with a real sealed value after bootstrap.
# Generate with:
#   kubectl create secret generic cloudflared-token \
#     --from-literal=token="$(terraform output -raw tunnel_token)" \
#     --dry-run=client -o yaml -n ecoplan \
#     | kubeseal --format yaml > deploy/manifests/cloudflared/sealed-secret.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: cloudflared-token
  namespace: ecoplan
spec:
  encryptedData:
    token: REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      name: cloudflared-token
      namespace: ecoplan
