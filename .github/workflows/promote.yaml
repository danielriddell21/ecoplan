name: Promote Release

on:
  release:
    types: [released]

jobs:
  retag:
    name: Retag image as latest
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Retag as latest
        run: |
          VERSION=${{ github.event.release.tag_name }}
          docker pull ghcr.io/ecoscan-uk/ecoscan:${VERSION}
          docker tag  ghcr.io/ecoscan-uk/ecoscan:${VERSION} ghcr.io/ecoscan-uk/ecoscan:latest
          docker push ghcr.io/ecoscan-uk/ecoscan:latest

  update-manifests:
    name: Pin version in deploy/manifests
    needs: retag
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          repository: ecoscan-uk/infra
          ref: trunk
          token: ${{ secrets.INFRA_GITHUB_TOKEN }}

      - name: Update image tag
        run: |
          VERSION=${{ github.event.release.tag_name }}
          sed -i "s|image: ghcr.io/ecoscan-uk/ecoscan:.*|image: ghcr.io/ecoscan-uk/ecoscan:${VERSION}|g" \
            deploy/manifests/service/deployment.yaml

      - name: Commit and push
        run: |
          VERSION=${{ github.event.release.tag_name }}
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add deploy/manifests/service/deployment.yaml
          git diff --staged --quiet || git commit -m "chore: deploy ${VERSION}"
          git push
